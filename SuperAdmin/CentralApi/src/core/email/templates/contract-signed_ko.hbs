<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>계약 서명 완료</title>
  <style>
    body { margin: 0; padding: 0; background-color: #f4f5f7; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
    .container { max-width: 600px; margin: 0 auto; background-color: #ffffff; }
    .header { background-color: #0d904f; padding: 24px 32px; text-align: center; }
    .header h1 { color: #ffffff; font-size: 20px; margin: 12px 0 0; font-weight: 600; }
    .body { padding: 32px; color: #333333; line-height: 1.6; }
    .info-table { width: 100%; border-collapse: collapse; margin: 16px 0; }
    .info-table td { padding: 8px 12px; border-bottom: 1px solid #e8eaed; font-size: 14px; }
    .info-table td:first-child { color: #666666; width: 40%; }
    .info-table td:last-child { font-weight: 600; }
    .success-badge { background-color: #e8f5e9; color: #2e7d32; text-align: center; padding: 16px; border-radius: 8px; margin: 16px 0; font-size: 16px; font-weight: 600; }
    .cta-button { display: inline-block; background-color: #0d904f; color: #ffffff; padding: 12px 32px; border-radius: 6px; text-decoration: none; font-weight: 600; font-size: 14px; margin: 16px 0; }
    .footer { background-color: #f4f5f7; padding: 24px 32px; text-align: center; font-size: 12px; color: #999999; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div style="color: #ffffff; font-size: 24px; font-weight: 700;">Meal Ticket</div>
      <h1>계약 서명 완료</h1>
    </div>
    <div class="body">
      <p><strong>{{recipientName}}</strong>님, 안녕하세요.</p>

      <div class="success-badge">계약 서명이 완료되었습니다</div>

      <p>모든 당사자의 서명이 완료되었습니다. 상세 내역은 아래와 같습니다.</p>

      <table class="info-table">
        <tr>
          <td>계약명</td>
          <td>{{contractTitle}}</td>
        </tr>
        <tr>
          <td>계약 번호</td>
          <td>{{contractId}}</td>
        </tr>
        <tr>
          <td>서명일</td>
          <td>{{signedDate}}</td>
        </tr>
        <tr>
          <td>서명자</td>
          <td>{{signerName}}</td>
        </tr>
        {{#if effectiveDate}}
        <tr>
          <td>효력 발생일</td>
          <td>{{effectiveDate}}</td>
        </tr>
        {{/if}}
        {{#if expirationDate}}
        <tr>
          <td>만료일</td>
          <td>{{expirationDate}}</td>
        </tr>
        {{/if}}
      </table>

      {{#if contractUrl}}
      <div style="text-align: center;">
        <a href="{{contractUrl}}" class="cta-button">서명된 계약서 다운로드</a>
      </div>
      {{/if}}

      <p>서명된 계약서 사본은 시스템에 보관됩니다. 도움이 필요하시면 언제든 연락해 주세요.</p>
    </div>
    <div class="footer">
      <p>Meal Ticket Platform &mdash; 기업 식대 관리 플랫폼</p>
      <p>본 이메일은 자동 발송되었습니다. 직접 회신하지 마세요.</p>
      <p>&copy; {{currentYear}} Meal Ticket Vietnam Co., Ltd. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
