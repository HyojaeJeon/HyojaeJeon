# PosUi src

`경로`: `/BrandPosApp/PosUi/src`

## 역할

- PosUi 소스 루트다.

## 반드시 지킬 규칙

- 상위 설계 문서와 `CLAUDE.md`의 계층 규칙을 따른다.
- `app`, `screens`, `store`, `bridge`, `shared` 책임을 섞지 않는다.
- 임시 구현을 장기 구조로 남기지 않는다.
