# SharedAssets

`경로`: `/SharedAssets`
상위 규칙: `../CLAUDE.md`

## 역할
- 모든 UI/앱이 함께 쓰는 정적 공용 자산 원본이다.

## 구현 규칙
- 디자인 시스템/이미지/정적 파일은 재사용 원본만 둔다.
- i18n 은 두지 않는다. 번역은 각 프로젝트 내부에서 관리한다.
- 앱별 임시 복사본을 여기서 새로 만들지 않는다.
- 텍스트/자산 변경은 여러 프로젝트에서 동시에 소비된다는 전제를 둔다.
